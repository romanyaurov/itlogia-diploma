import { StaticImagePathPipe } from './static-image-path.pipe';

describe('StaticImagePathPipe', () => {
  it('create an instance', () => {
    const pipe = new StaticImagePathPipe();
    expect(pipe).toBeTruthy();
  });
});
